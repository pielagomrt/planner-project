<div class="d-flex float-left">
  <%= link_to edit_category_path(@category), class: 'btn btn-sm btn-warning' do %>
    <span>✏</span>
  <% end %> 
  <%= link_to category_path(@category), class: 'btn btn-sm btn-danger', method: :delete, data: { confirm: 'Delete this CATEGORY?' } do %>
    <span>🗑</span>
  <% end %>
</div>

<div class="d-flex float-right">
  <%= link_to 'Back To Categories', categories_path, class: "btn btn-sm btn-default" %>
</div>

<br><br><br>

<div class="mt-0 mb-0 text-center title-size">
  <%= @category.name.upcase %>
</div>

<div class="text-muted mt-2 mb-4 text-center display-6 small">
  <%= @category.created_at.strftime("Created on %d-%^b-%Y, %l:%M %p UTC") %>
  • Updated <%= time_ago_in_words(@category.updated_at) %> ago
</div><hr>

